name: Hello World

# când se rulează
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Print Hello
        run: echo "Hello, GitHub Actions 🚀"
